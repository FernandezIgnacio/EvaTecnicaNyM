import {AlertDismissible} from './alert'
function Buttin(curso, usuario){
    const curso = {};

    if (!curso[curso]) {
        curso[curso] = [];
    }
  
  curso[curso].push(usuario);
  
  <AlertDismissible>("Te has inscrito en el curso " + curso);</AlertDismissible>
}
export default Buttin