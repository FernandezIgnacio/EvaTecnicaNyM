export function Inscrito(curso, usuario){
    const curso = {};

    if (!curso[curso]) {
        curso[curso] = [];
    }
  
  curso[curso].push(usuario);
  
  alert("Te has inscrito en el curso " + curso);
}